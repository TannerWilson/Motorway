const config = {
    user: 'user', 
    password: 'password', 
    host: 'localhost', 
    database: 'motorway', 
    port: '5432', 
    // connectionString: `postgres://${this.user}:${this.password}@${this.host}:${this.port}/${this.database}`, // e.g. postgres://user:password@host:5432/database
}

export default config;